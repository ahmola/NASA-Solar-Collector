services:
  # LLM Engine
  ollama:
    image: ollama/ollama:latest
    container_name: local-ollama
    volumes:
      - ollama_data:/root/.ollama
    ports:
      - "11434:11434"
    # GPU config
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # MCP
  mcp-server:
    build: ./mcp-server
    container_name: mcp-server
    ports:
      - "8000:8000"

  # AI Agent
  hermes-agent:
    image: nousresearch/hermes-agent:latest
    container_name: hermes-agent
    restart: unless-stopped
    ports:
      - "8642:8642"
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434
      - MCP_SERVER_URL=http://mcp-server:8000
    volumes:
      - ./hermes:/app/hermes
    depends_on:
      - ollama
      - mcp-server

  # Web UI
  agent-gui:
    build: ./frontend
    container_name: agent-gui
    ports:
      - "3000:3000"
    environment:
      - AGENT_API_URL=http://hermes-agent:8642
    depends_on:
      - hermes-agent

volumes:
  ollama_data:
